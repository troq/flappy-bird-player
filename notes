for significant pipe, after found, check if there is a pipe below it w/ an x coordinate close to the
significant pipe's x coord (i.e. -10), that way, if top pipe is slightly forward and is not cut off
by the x threshold and the bottom pipe is, the bottom pipe can be selected
